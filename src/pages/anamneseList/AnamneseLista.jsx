
import React, { useEffect, useState } from 'react';
import { Table, Container, Spinner, Alert } from 'react-bootstrap';

const AnamneseLista = () => {
    /*const [anamneseLista, setAnamneseLista] = useState([]);
    const [loading, setLoading] = useState(true);
    const [error, setError] = useState(null);

    const fetchAnamnese = async () => {
        try {
            const response = await fetch('http://127.0.0.1:3002/anamnese', {
                method: 'GET',
                headers: {
                    'Content-Type': 'application/json',
                },
            });
            
            if (!response.ok) {
                throw new Error('Erro ao carregar as anamneses');
            }
            
            const data = await response.json(); // Processa a resposta como JSON
            setAnamneseLista(data); // Atualiza a lista com os dados retornados
        } catch (err) {
            setError('Erro ao carregar as anamneses');
        } finally {
            setLoading(false);
        }
    }; */

    return (
        
            <h1>TELA EM PAUSA POR CONTA DE VALIDAÇÂO DE EMAIL</h1>
        
    );
};

export default AnamneseLista;
